android {
    compileSdkVersion(27)
    buildToolsVersion("27.0.3")

    defaultConfig {
        minSdkVersion(19)
        targetSdkVersion(27)

        applicationId = "ru.endlesscode.miet.orioks"
        versionCode = 1
        versionName = "0.1.0"
    }
}
