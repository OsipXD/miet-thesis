plugins {
    java
}

repositories {
    mavenCentral() // Подключение репозитория |\label{line:repo}|
}

dependencies {
    testImplementation("junit:junit:4.12") // Подключение зависимости |\label{line:junit}|
}
